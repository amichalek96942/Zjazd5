Zadanie (0.10 pkt)
Na wejściu podane są dwie liczby: N oraz M. Program generuje N pierwszych liczb Fibonacciego, każda wyświetlana jest modulo M (reszta z dzielenia przez M). 

Ciąg Fibonacciego definiowany jest jako: F(0)=0, F(1)=1, F(n)=F(n-1)+F(n-2).


#include <stdio.h>

int main()
{

    int N = 0;
    int M = 0;
    scanf("%d %d", &N, &M);

    int a = 0; // F(0)
    int b = 1; // F(1)

    for (int i = 0; i < N; i++)
    {
        printf("%d ", a % M);

        int next = a + b;
        a = b;
        b = next;
    }

    return 0;
}

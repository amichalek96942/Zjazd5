Zadanie (0.10 pkt)
Na wejściu podana jest liczba N (długość ciągu), następnie N liczb (tylko 0 lub 1). Program kompresuje ciąg zapisując długości kolejnych serii. Kompresja zaczyna się od serii zer (jeśli ciąg zaczyna się od 1, pierwsza seria ma długość 0).Format wyjścia: liczby oddzielone spacjami, na przemian długości serii zer i jedynek.


#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int prev = 0;          // zaczynamy od serii zer
    int count = 0;
    int x;

    for (int i = 0; i < N; i++) {
        scanf("%d", &x);

        if (x == prev) {
            count++;
        } else {
            printf("%d ", count);
            count = 1;
            prev = x;
        }
    }

    // wypisanie ostatniej serii
    printf("%d", count);

    return 0;
}
